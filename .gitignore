node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Testing
coverage/

# TypeScript
*.tsbuildinfo

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code local settings
.claude/settings.local.json
.claude/project-state.md

# Downloadable org data — regenerated by `pnpm data:clubs` (and on every deploy).
# Untracked to avoid huge diff churn; the geocode caches (geocodes.json,
# club-geocodes.json) are intentionally NOT ignored — they're expensive to rebuild.
/static/data/clubs-by-district.json
/static/data/districts.json
/static/data/organizations-all.json
